//Verifique se todas as variáveis de um conjunto são falsas (usando &&)

let a = false
let b = false
let c = false


console.log(( a == b && a ==c && a==false) && "Todas as variáveis são falsas" || "Alguma variável é verdadeira")

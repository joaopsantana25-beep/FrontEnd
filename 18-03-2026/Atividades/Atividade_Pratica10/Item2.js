/*
2. Crie um código que converta a string "APRENDER JS" em letras
minúsculas.
*/

let mensagem = "APRENDER JS";

//Comando para transformar a string em apenas caracteres minúsculos
mensagem = mensagem.toLowerCase();

console.log(mensagem);

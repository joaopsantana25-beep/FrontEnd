/*Verifique se pelo menos uma das duas variáveis é true usando ||.*/

let valor1 = false
let valor2 = true

console.log((valor1 || valor2) && "Pelo menos 1 é verdadeiro" || "Ambos os valores são falso")
